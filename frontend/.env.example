# Use Go API via Vite proxy (backend on :8080)
VITE_USE_API=true

# Google Sign-In — same Client ID as GOOGLE_CLIENT_ID in backend/.env
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Or call API directly (requires CORS on backend):
# VITE_API_URL=http://localhost:8080/api
